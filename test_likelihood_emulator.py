# Test file removed: skip to avoid importing legacy 'probabilidade' package
import pytest

pytest.skip("Legacy root test removed — use tests/test_likelihood_emulator.py instead", allow_module_level=True)
